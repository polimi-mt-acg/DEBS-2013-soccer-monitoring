#ifndef STREAM_TYPES_H
#define STREAM_TYPES_H

namespace game {
/**
 * A type for using a file stream in dataset parsing.
 */
struct file_stream {};

/**
 * A type for using a string stream in dataset parsing.
 */
struct string_stream {};
} // namespace game

#endif